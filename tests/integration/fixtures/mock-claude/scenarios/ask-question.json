{
  "name": "ask-question",
  "description": "AskUserQuestion routed through the real MCP permission server + decision bridge (run with skipPermissions: false). The user's option reaction resolves the pending call and the answers ride back via updatedInput, exactly like production on a modern CLI.",
  "turns": [
    {
      "steps": [
        { "kind": "text", "text": "I have a question about how to proceed." },
        {
          "kind": "question",
          "questions": [
            {
              "question": "Which approach would you prefer?",
              "header": "Approach",
              "options": [
                { "label": "Option A", "description": "First approach" },
                { "label": "Option B", "description": "Second approach" }
              ],
              "multiSelect": false
            }
          ]
        },
        { "kind": "text", "text": "Thanks — proceeding with your choice." },
        { "kind": "result", "resultText": "Question flow complete." }
      ]
    }
  ]
}
