{
  "name": "simple-response",
  "description": "One text turn ending in a success result; process exits afterwards (suites assert the session ends)",
  "exitAtEnd": true,
  "turns": [
    {
      "steps": [
        { "kind": "text", "text": "Hello! I received your message. This is a mock response for testing." },
        { "kind": "result", "resultText": "Hello! I received your message. This is a mock response for testing." }
      ]
    }
  ]
}
