{
  "name": "permission-request",
  "description": "Write tool turn. Bypass mode: runs straight through with a tool_result in a user event. Interactive mode: the tool is gated through the real MCP permission server first.",
  "exitAtEnd": true,
  "turns": [
    {
      "steps": [
        { "kind": "text", "text": "I'll write that to a file for you." },
        {
          "kind": "tool",
          "name": "Write",
          "input": { "file_path": "/tmp/test-output.txt", "content": "Test content for integration testing" },
          "result": "File created successfully at: /tmp/test-output.txt"
        },
        { "kind": "text", "text": "Done! The file has been written successfully." },
        { "kind": "result", "resultText": "Done! The file has been written successfully." }
      ]
    }
  ]
}
